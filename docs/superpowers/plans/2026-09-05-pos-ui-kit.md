# POS UI Kit & Ảnh sản phẩm Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng bộ UI kit dùng lại được tại `src/components/kit/`, thay bảng màu xám bằng bảng có màu, và làm sống `Product.imageUrl` — upload được ảnh, hiện ở POS, mất mạng vẫn thấy ảnh.

**Architecture:** Sáu tầng, tầng dưới không biết gì về tầng trên: token màu trong `globals.css` → primitive shadcn trong `src/components/ui/` → kit thuần trình bày trong `src/components/kit/` → đường đi của ảnh (Server Action → sharp → `public/uploads/`) → service worker cache-first cho `/uploads/` → trang gallery `/dev/kit`. Kit **không** import Prisma, store, hay Server Action; nó nhận props và phát callback, nên copy sang dự án khác được.

**Tech Stack:** Kế thừa Plan 1 & 2 (Next 16 App Router, React 19, TS strict, Tailwind 4, shadcn style `base-nova` trên `@base-ui/react`, Prisma 6 + SQLite, Zod 4, Vitest 4 jsdom, Playwright 1.59), thêm `sharp` để resize ảnh.

**Spec:** `docs/superpowers/specs/2026-09-05-pos-ui-kit-design.md`

## Global Constraints

- **Tiền là số nguyên VND.** Mọi hiển thị tiền đi qua `formatVnd` từ `@/lib/money`. Không tự định dạng tiền ở bất kỳ component nào.
- **Số lượng và tồn kho là số thực.**
- **Route và tên code bằng tiếng Anh; mọi chữ hiển thị cho người dùng bằng tiếng Việt.**
- **Kit thuần trình bày.** File nào trong `src/components/kit/` import `@/server/*`, `@/stores/*`, hay `next/cache` là sai — đó là ranh giới khiến kit dùng lại được.
- **Giữ nguyên bố cục.** Không đổi bố cục 2 cột của `/pos`, không đổi nghiệp vụ tính tiền, `createOrder`, hàng đợi offline, phím tắt.
- **Prisma schema không đổi.** `imageUrl` đã tồn tại trong `model Product`.
- **Không cache `/api/` và `/admin` trong service worker** — nguyên tắc từ Plan 2, giữ nguyên.
- Prefer Server Components; chỉ thêm `"use client"` khi thật sự cần (theo `AGENTS.md`).
- Mọi payload API và Server Action validate bằng Zod (theo `AGENTS.md`).
- Package manager là **pnpm**. Test chỉ đặt trong `tests/**` (Vitest chỉ include thư mục này) và `e2e/**`. Alias `@/*` → `./src/*`.
- Ngưỡng "sắp hết" là hằng số trong kit, mặc định `5`, ghi đè qua prop. Không thêm cột vào schema.
- Ghi file lúc chạy nghĩa là cần filesystem ghi được: chạy với `next start` self-host, **không** chạy trên Vercel serverless.

---

## File Structure

**Create — kit (thuần, không phụ thuộc framework ngoài React):**

- `src/components/kit/money.tsx` — hiển thị tiền VND, 3 cỡ
- `src/components/kit/stock-badge.tsx` — badge tồn kho 3 mức
- `src/components/kit/touch-button.tsx` — nút ép chiều cao chạm
- `src/components/kit/chip-toggle.tsx` — chip danh mục bật/tắt
- `src/components/kit/product-image.tsx` — ảnh + dự phòng chữ cái đầu
- `src/components/kit/avatar-color.ts` — hàm thuần sinh màu dự phòng từ tên
- `src/components/kit/product-tile.tsx` — thẻ sản phẩm đều cỡ
- `src/components/kit/search-field.tsx` — ô tìm có icon + nút xoá
- `src/components/kit/result-list.tsx` — `ResultList` + `ResultRow`
- `src/components/kit/page-header.tsx` — tiêu đề trang admin
- `src/components/kit/empty-state.tsx` — trạng thái rỗng
- `src/components/kit/data-table-shell.tsx` — vỏ bảng admin
- `src/components/kit/stat-tile.tsx` — ô số liệu
- `src/components/kit/collapsible-form-card.tsx` — card form gập được
- `src/components/kit/index.ts` — barrel export

**Create — primitive:**

- `src/components/ui/collapsible.tsx` — từ registry shadcn

**Create — ảnh:**

- `src/server/products/save-image.ts` — kiểm tra, resize, ghi file, xoá file cũ

**Create — gallery:**

- `src/app/dev/kit/page.tsx` — trang xem toàn bộ kit

**Modify:**

- `src/app/globals.css` — token màu, `--touch-target`, thang chữ display
- `components.json` — `baseColor` → `blue`
- `package.json` — thêm `sharp`
- `pnpm-workspace.yaml` — chuyển `sharp` sang `onlyBuiltDependencies`
- `.gitignore` — thêm `public/uploads/`
- `src/lib/search/types.ts` — thêm `imageUrl` vào `SearchableProduct`
- `src/app/api/catalog/route.ts` — `select` thêm `imageUrl`
- `src/server/products/save-product.ts` — `SaveProductInput` nhận `imageUrl`
- `src/app/admin/products/actions.ts` — nhận file ảnh
- `src/app/admin/products/product-form.tsx` — ô chọn ảnh + xem trước
- `src/app/admin/products/page.tsx` — dùng kit
- `src/components/pos/product-search.tsx` — dùng kit
- `src/components/pos/category-grid.tsx` — dùng kit
- `public/sw.js` — cache-first cho `/uploads/`
- `prisma/seed.ts` — vài sản phẩm mẫu có `imageUrl`

**Test:**

- `tests/components/kit/*.test.tsx` — một file cho mỗi component có logic
- `tests/server/products/save-image.test.ts`
- `e2e/admin-product-image.spec.ts`
- `e2e/kit-gallery.spec.ts`

---

## Task 1: Bảng màu và token chạm

Đây là gốc rễ của "chưa đẹp": mọi token hiện tại có chroma = 0.

**Files:**

- Modify: `src/app/globals.css`
- Modify: `components.json:11`
- Test: `tests/config/theme-tokens.test.ts`

**Interfaces:**

- Consumes: không có
- Produces: các biến CSS `--primary`, `--success`, `--success-foreground`, `--warning`, `--warning-foreground`, `--info`, `--info-foreground`, `--touch-target`; và class Tailwind `bg-success`, `text-success-foreground`, `bg-warning`, `text-warning-foreground`, `bg-info`, `text-info-foreground`, `min-h-touch`

- [ ] **Step 1: Viết test kiểm mọi token semantic có đủ ở cả hai theme**

Tạo `tests/config/theme-tokens.test.ts`:

```ts
import { readFileSync } from "node:fs";
import path from "node:path";

import { describe, expect, it } from "vitest";

const css = readFileSync(
  path.resolve(__dirname, "../../src/app/globals.css"),
  "utf8",
);

/** Lay noi dung mot block CSS theo selector, vd ":root" hoac ".dark". */
function block(selector: string): string {
  const start = css.indexOf(`${selector} {`);
  if (start === -1) throw new Error(`Khong tim thay block ${selector}`);
  const end = css.indexOf("\n}", start);
  return css.slice(start, end);
}

function tokens(selector: string): Map<string, string> {
  const map = new Map<string, string>();
  for (const line of block(selector).split("\n")) {
    const match = line.match(/^\s*(--[\w-]+)\s*:\s*(.+);\s*$/);
    if (match) map.set(match[1], match[2]);
  }
  return map;
}

const SEMANTIC = [
  "--primary",
  "--primary-foreground",
  "--success",
  "--success-foreground",
  "--warning",
  "--warning-foreground",
  "--info",
  "--info-foreground",
];

describe("theme tokens", () => {
  const light = tokens(":root");
  const dark = tokens(".dark");

  it.each(SEMANTIC)("dinh nghia %s o ca light lan dark", (token) => {
    expect(light.has(token)).toBe(true);
    expect(dark.has(token)).toBe(true);
  });

  it("khong con la bang mau xam — primary phai co chroma", () => {
    // oklch(L C H) — C la so thu hai. Bang xam cu co C = 0 o moi token.
    const chroma = (value: string) =>
      Number(value.match(/oklch\(\s*[\d.]+\s+([\d.]+)/)?.[1] ?? "0");

    expect(chroma(light.get("--primary")!)).toBeGreaterThan(0.05);
    expect(chroma(dark.get("--primary")!)).toBeGreaterThan(0.05);
  });

  it("dinh nghia --touch-target dat 44px", () => {
    expect(light.get("--touch-target")).toBe("2.75rem");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/config/theme-tokens.test.ts`
Expected: FAIL — `--success` chưa tồn tại ở `:root`, và chroma của `--primary` là `0`.

- [ ] **Step 3: Khai báo token mới trong `@theme inline`**

Trong `src/app/globals.css`, thêm vào khối `@theme inline` (ngay sau dòng `--color-destructive: var(--destructive);`):

```css
--color-destructive-foreground: var(--destructive-foreground);
--color-success: var(--success);
--color-success-foreground: var(--success-foreground);
--color-warning: var(--warning);
--color-warning-foreground: var(--warning-foreground);
--color-info: var(--info);
--color-info-foreground: var(--info-foreground);
--spacing-touch: var(--touch-target);
```

- [ ] **Step 4: Thay bảng màu light trong `:root`**

Trong `src/app/globals.css`, thay các dòng tương ứng của khối `:root`:

```css
--primary: oklch(0.55 0.19 255);
--primary-foreground: oklch(0.985 0 0);
--destructive: oklch(0.577 0.245 27.325);
--destructive-foreground: oklch(0.985 0 0);
--success: oklch(0.55 0.14 150);
--success-foreground: oklch(0.985 0 0);
--warning: oklch(0.65 0.15 70);
--warning-foreground: oklch(0.145 0 0);
--info: oklch(0.6 0.14 230);
--info-foreground: oklch(0.985 0 0);
--ring: oklch(0.55 0.19 255);
--border: oklch(0.9 0.005 255);
--input: oklch(0.9 0.005 255);
--chart-1: oklch(0.55 0.19 255);
--chart-2: oklch(0.55 0.14 150);
--chart-3: oklch(0.65 0.15 70);
--chart-4: oklch(0.6 0.16 300);
--chart-5: oklch(0.6 0.17 20);
--touch-target: 2.75rem;
```

- [ ] **Step 5: Thay bảng màu dark trong `.dark`**

Trong `src/app/globals.css`, thay các dòng tương ứng của khối `.dark`:

```css
--primary: oklch(0.65 0.18 255);
--primary-foreground: oklch(0.145 0 0);
--destructive: oklch(0.704 0.191 22.216);
--destructive-foreground: oklch(0.145 0 0);
--success: oklch(0.7 0.15 150);
--success-foreground: oklch(0.145 0 0);
--warning: oklch(0.78 0.15 75);
--warning-foreground: oklch(0.145 0 0);
--info: oklch(0.7 0.13 230);
--info-foreground: oklch(0.145 0 0);
--ring: oklch(0.65 0.18 255);
--chart-1: oklch(0.65 0.18 255);
--chart-2: oklch(0.7 0.15 150);
--chart-3: oklch(0.78 0.15 75);
--chart-4: oklch(0.7 0.16 300);
--chart-5: oklch(0.7 0.17 20);
--touch-target: 2.75rem;
```

Lưu ý: `--touch-target` lặp lại ở `.dark` để `--spacing-touch` không rỗng nếu
có ai nạp riêng block dark. Test ở Step 1 chỉ đọc nó ở `:root`.

- [ ] **Step 6: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/config/theme-tokens.test.ts`
Expected: PASS — 11 test.

- [ ] **Step 7: Để nguyên `baseColor`**

Không đổi `components.json`. `baseColor` chỉ nhận một tập cố định do registry
quy định (`gray`, `neutral`, `slate`, `stone`, `zinc`) — không nhận màu thật;
đặt `"blue"` làm mọi lệnh `shadcn add` về sau chết với lỗi
`The item at .../r/colors/blue.json was not found`.

Việc này cũng không cần thiết: CLI chỉ ghi bảng màu lúc `init`, còn `add` thì
không đụng vào token. Bảng màu ở Step 4 và 5 là nguồn sự thật duy nhất.

- [ ] **Step 8: Commit**

```bash
git add src/app/globals.css components.json tests/config/theme-tokens.test.ts
git commit -m "feat(theme): replace grayscale tokens with a colored palette"
```

---

## Task 2: Primitive `collapsible`

Kit cần đúng một primitive chưa có: `collapsible`, dùng cho `CollapsibleFormCard`.

**Files:**

- Create: `src/components/ui/collapsible.tsx`

**Interfaces:**

- Consumes: không có
- Produces: `Collapsible`, `CollapsibleTrigger`, `CollapsiblePanel` từ `@/components/ui/collapsible`

- [ ] **Step 1: Thêm primitive bằng shadcn CLI**

Run: `pnpm dlx shadcn@latest add collapsible`
Expected: tạo `src/components/ui/collapsible.tsx`.

Nếu máy không có mạng, tạo tay file `src/components/ui/collapsible.tsx`:

```tsx
"use client";

import { Collapsible as CollapsiblePrimitive } from "@base-ui/react/collapsible";

function Collapsible({
  ...props
}: React.ComponentProps<typeof CollapsiblePrimitive.Root>) {
  return <CollapsiblePrimitive.Root data-slot="collapsible" {...props} />;
}

function CollapsibleTrigger({
  ...props
}: React.ComponentProps<typeof CollapsiblePrimitive.Trigger>) {
  return (
    <CollapsiblePrimitive.Trigger data-slot="collapsible-trigger" {...props} />
  );
}

function CollapsiblePanel({
  ...props
}: React.ComponentProps<typeof CollapsiblePrimitive.Panel>) {
  return (
    <CollapsiblePrimitive.Panel data-slot="collapsible-panel" {...props} />
  );
}

export { Collapsible, CollapsibleTrigger, CollapsiblePanel };
```

- [ ] **Step 2: Kiểm typecheck**

Run: `pnpm typecheck`
Expected: không lỗi.

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/collapsible.tsx
git commit -m "feat(ui): add shadcn collapsible primitive"
```

---

## Task 3: `Money` — hiển thị tiền

**Files:**

- Create: `src/components/kit/money.tsx`
- Test: `tests/components/kit/money.test.tsx`

**Interfaces:**

- Consumes: `formatVnd` từ `@/lib/money`; `cn` từ `@/lib/utils`
- Produces: `Money` — `(props: { amount: number; size?: "sm" | "base" | "display"; className?: string }) => JSX.Element`

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/money.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { Money } from "@/components/kit/money";

describe("Money", () => {
  it("dinh dang so tien theo dinh dang VND", () => {
    render(<Money amount={15000} />);
    expect(screen.getByText("15.000")).toBeInTheDocument();
  });

  it("luon dung tabular-nums de cot so thang hang", () => {
    const { container } = render(<Money amount={1000} />);
    expect(container.firstChild).toHaveClass("tabular-nums");
  });

  it("co cach doc cho trinh doc man hinh", () => {
    render(<Money amount={15000} />);
    expect(screen.getByText("15.000")).toHaveAttribute("aria-label", "15.000");
  });

  it("size display to hon size base", () => {
    const { container: base } = render(<Money amount={1} size="base" />);
    const { container: display } = render(<Money amount={1} size="display" />);
    expect(base.firstChild).toHaveClass("text-base");
    expect(display.firstChild).toHaveClass("text-5xl");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/money.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/money'`.

- [ ] **Step 3: Viết component**

Tạo `src/components/kit/money.tsx`:

```tsx
import { formatVnd } from "@/lib/money";
import { cn } from "@/lib/utils";

const SIZES = {
  sm: "text-sm",
  base: "text-base",
  /** Cho "tien thoi lai (chu rat to)" — Spec 1 muc 5. */
  display: "text-5xl",
} as const;

interface MoneyProps {
  amount: number;
  size?: keyof typeof SIZES;
  className?: string;
}

/** Moi cho hien tien deu di qua day, de dinh dang khong bao gio lech nhau. */
export function Money({ amount, size = "base", className }: MoneyProps) {
  const text = formatVnd(amount);

  return (
    <span
      aria-label={text}
      className={cn(
        "font-semibold tabular-nums",
        SIZES[size],
        size === "display" && "font-bold tracking-tight",
        className,
      )}
    >
      {text}
    </span>
  );
}
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/money.test.tsx`
Expected: PASS — 4 test.

- [ ] **Step 5: Commit**

```bash
git add src/components/kit/money.tsx tests/components/kit/money.test.tsx
git commit -m "feat(kit): add Money display component"
```

---

## Task 4: `StockBadge` — tồn kho liếc là thấy

**Files:**

- Create: `src/components/kit/stock-badge.tsx`
- Test: `tests/components/kit/stock-badge.test.tsx`

**Interfaces:**

- Consumes: `Badge` từ `@/components/ui/badge`; `cn` từ `@/lib/utils`
- Produces: `StockBadge` — `(props: { stock: number; unit: string; threshold?: number; className?: string }) => JSX.Element`; `LOW_STOCK_THRESHOLD = 5`; `stockLevel(stock: number, threshold?: number): "out" | "low" | "ok"`

- [ ] **Step 1: Viết test — chú ý các ngưỡng biên**

Tạo `tests/components/kit/stock-badge.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import {
  LOW_STOCK_THRESHOLD,
  StockBadge,
  stockLevel,
} from "@/components/kit/stock-badge";

describe("stockLevel", () => {
  it("nguong mac dinh la 5", () => {
    expect(LOW_STOCK_THRESHOLD).toBe(5);
  });

  it.each([
    [-1, "out"],
    [0, "out"],
    [0.5, "low"],
    [5, "low"],
    [5.0001, "ok"],
    [6, "ok"],
  ])("stock %s -> %s", (stock, expected) => {
    expect(stockLevel(stock)).toBe(expected);
  });

  it("nhan nguong tu ben ngoai", () => {
    expect(stockLevel(8, 10)).toBe("low");
    expect(stockLevel(11, 10)).toBe("ok");
  });
});

describe("StockBadge", () => {
  it("het hang thi noi 'Het hang', khong noi so", () => {
    render(<StockBadge stock={0} unit="cái" />);
    expect(screen.getByText("Hết hàng")).toBeInTheDocument();
  });

  it("ton am van la het hang, va hien so am de ke toan thay", () => {
    render(<StockBadge stock={-3} unit="cái" />);
    expect(screen.getByText("Hết hàng (-3)")).toBeInTheDocument();
  });

  it("con hang thi hien so kem don vi", () => {
    render(<StockBadge stock={50} unit="cái" />);
    expect(screen.getByText("Còn 50 cái")).toBeInTheDocument();
  });

  it("sap het thi doi mau canh bao", () => {
    const { container } = render(<StockBadge stock={2} unit="mét" />);
    expect(screen.getByText("Còn 2 mét")).toBeInTheDocument();
    expect(container.firstChild).toHaveClass("bg-warning");
  });

  it("so thuc khong bi lam tron", () => {
    render(<StockBadge stock={2.5} unit="mét" />);
    expect(screen.getByText("Còn 2.5 mét")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/stock-badge.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/stock-badge'`.

- [ ] **Step 3: Viết component**

Tạo `src/components/kit/stock-badge.tsx`:

```tsx
import { Badge } from "@/components/ui/badge";
import { cn } from "@/lib/utils";

/** Cua hang chua khai bao nguong rieng cho tung mat hang — dung mot so chung. */
export const LOW_STOCK_THRESHOLD = 5;

export type StockLevel = "out" | "low" | "ok";

export function stockLevel(
  stock: number,
  threshold: number = LOW_STOCK_THRESHOLD,
): StockLevel {
  if (stock <= 0) return "out";
  if (stock <= threshold) return "low";
  return "ok";
}

const LEVEL_CLASS: Record<StockLevel, string> = {
  out: "bg-destructive text-white",
  low: "bg-warning text-warning-foreground",
  ok: "bg-success text-success-foreground",
};

interface StockBadgeProps {
  stock: number;
  unit: string;
  threshold?: number;
  className?: string;
}

/**
 * Thu ngan phai liec la thay — nen ba muc dung ba mau khac han nhau,
 * khong chi khac sac do dam.
 */
export function StockBadge({
  stock,
  unit,
  threshold,
  className,
}: StockBadgeProps) {
  const level = stockLevel(stock, threshold);

  const label =
    level === "out"
      ? stock < 0
        ? `Hết hàng (${stock})`
        : "Hết hàng"
      : `Còn ${stock} ${unit}`;

  return (
    <Badge className={cn(LEVEL_CLASS[level], "tabular-nums", className)}>
      {label}
    </Badge>
  );
}
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/stock-badge.test.tsx`
Expected: PASS — 11 test.

- [ ] **Step 5: Commit**

```bash
git add src/components/kit/stock-badge.tsx tests/components/kit/stock-badge.test.tsx
git commit -m "feat(kit): add StockBadge with three stock levels"
```

---

## Task 5: `TouchButton` và `ChipToggle` — nút bấm trúng bằng ngón tay

Spec 1 mục 5: "bấm bằng ngón tay trên tablet phải trúng". Chip danh mục hiện tại cao ~40px, dưới ngưỡng.

**Files:**

- Create: `src/components/kit/touch-button.tsx`
- Create: `src/components/kit/chip-toggle.tsx`
- Test: `tests/components/kit/chip-toggle.test.tsx`

**Interfaces:**

- Consumes: `Button` từ `@/components/ui/button`; `cn` từ `@/lib/utils`
- Produces: `TouchButton` — cùng props với `Button`, thêm `min-h-touch`; `ChipToggle` — `(props: { label: string; selected: boolean; onToggle: () => void; className?: string }) => JSX.Element`

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/chip-toggle.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";

import { ChipToggle } from "@/components/kit/chip-toggle";
import { TouchButton } from "@/components/kit/touch-button";

describe("TouchButton", () => {
  it("ep chieu cao toi thieu bang o chạm", () => {
    render(<TouchButton>Thanh toán</TouchButton>);
    expect(screen.getByRole("button")).toHaveClass("min-h-touch");
  });
});

describe("ChipToggle", () => {
  it("dat chieu cao o cham de bam ngon tay trung", () => {
    render(<ChipToggle label="Tạp hoá" selected={false} onToggle={() => {}} />);
    expect(screen.getByRole("button")).toHaveClass("min-h-touch");
  });

  it("bao trang thai chon cho trinh doc man hinh", () => {
    render(<ChipToggle label="Tạp hoá" selected onToggle={() => {}} />);
    expect(screen.getByRole("button")).toHaveAttribute("aria-pressed", "true");
  });

  it("goi onToggle khi bam", async () => {
    const onToggle = vi.fn();
    render(
      <ChipToggle label="Phụ tùng" selected={false} onToggle={onToggle} />,
    );
    await userEvent.click(screen.getByRole("button"));
    expect(onToggle).toHaveBeenCalledOnce();
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/chip-toggle.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/touch-button'`.

- [ ] **Step 3: Viết `TouchButton`**

Tạo `src/components/kit/touch-button.tsx`:

```tsx
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

type TouchButtonProps = React.ComponentProps<typeof Button>;

/**
 * Man hinh ban dung ca ngay tren tablet — moi nut phai cao it nhat
 * --touch-target (44px). Bọc Button de khong ai quen dat lai chieu cao.
 */
export function TouchButton({ className, ...props }: TouchButtonProps) {
  return <Button className={cn("min-h-touch", className)} {...props} />;
}
```

- [ ] **Step 4: Viết `ChipToggle`**

Tạo `src/components/kit/chip-toggle.tsx`:

```tsx
"use client";

import { TouchButton } from "@/components/kit/touch-button";
import { cn } from "@/lib/utils";

interface ChipToggleProps {
  label: string;
  selected: boolean;
  onToggle: () => void;
  className?: string;
}

/** Chip danh muc: bam lan nua de bo chon, nen la toggle chu khong phai tab. */
export function ChipToggle({
  label,
  selected,
  onToggle,
  className,
}: ChipToggleProps) {
  return (
    <TouchButton
      type="button"
      aria-pressed={selected}
      variant={selected ? "default" : "outline"}
      onClick={onToggle}
      className={cn("rounded-full px-5 text-base", className)}
    >
      {label}
    </TouchButton>
  );
}
```

- [ ] **Step 5: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/chip-toggle.test.tsx`
Expected: PASS — 4 test.

- [ ] **Step 6: Commit**

```bash
git add src/components/kit/touch-button.tsx src/components/kit/chip-toggle.tsx tests/components/kit/chip-toggle.test.tsx
git commit -m "feat(kit): add TouchButton and ChipToggle with 44px targets"
```

---

## Task 6: `ProductImage` — ảnh và ảnh dự phòng

Phần lớn sản phẩm sẽ chưa có ảnh trong thời gian dài. Ảnh dự phòng phải đẹp,
không được là ô xám trống, và phải **ổn định** — cùng một tên luôn ra cùng một màu.

**Files:**

- Create: `src/components/kit/avatar-color.ts`
- Create: `src/components/kit/product-image.tsx`
- Test: `tests/components/kit/product-image.test.tsx`

**Interfaces:**

- Consumes: `cn` từ `@/lib/utils`
- Produces: `avatarHue(name: string): number` (0–359); `initials(name: string): string`; `ProductImage` — `(props: { src?: string | null; name: string; size?: number; className?: string }) => JSX.Element`

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/product-image.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";

import { avatarHue, initials } from "@/components/kit/avatar-color";
import { ProductImage } from "@/components/kit/product-image";

describe("avatarHue", () => {
  it("cung mot ten luon ra cung mot mau", () => {
    expect(avatarHue("Bugi Wave")).toBe(avatarHue("Bugi Wave"));
  });

  it("ten khac nhau thuong ra mau khac nhau", () => {
    expect(avatarHue("Bugi Wave")).not.toBe(avatarHue("Nhớt Castrol"));
  });

  it("luon nam trong khoang mau hop le", () => {
    for (const name of ["a", "Bugi", "Nhớt 4T", "", "Dây điện 2 mét"]) {
      const hue = avatarHue(name);
      expect(hue).toBeGreaterThanOrEqual(0);
      expect(hue).toBeLessThan(360);
    }
  });
});

describe("initials", () => {
  it("lay chu cai dau cua hai tu dau", () => {
    expect(initials("Bugi Wave")).toBe("BW");
  });

  it("ten mot tu thi lay mot chu", () => {
    expect(initials("Bugi")).toBe("B");
  });

  it("giu dau tieng Viet", () => {
    expect(initials("Nhớt Castrol")).toBe("NC");
  });

  it("ten rong khong lam vo", () => {
    expect(initials("")).toBe("?");
  });
});

describe("ProductImage", () => {
  it("hien anh khi co duong dan", () => {
    render(<ProductImage src="/uploads/abc.webp" name="Bugi Wave" />);
    expect(screen.getByRole("img", { name: "Bugi Wave" })).toHaveAttribute(
      "src",
      "/uploads/abc.webp",
    );
  });

  it("khong co anh thi hien chu cai dau, khong hien the img rong", () => {
    render(<ProductImage src={null} name="Bugi Wave" />);
    expect(screen.queryByRole("img")).not.toBeInTheDocument();
    expect(screen.getByText("BW")).toBeInTheDocument();
  });

  it("anh du phong duoc an khoi trinh doc man hinh", () => {
    const { container } = render(<ProductImage src={null} name="Bugi Wave" />);
    expect(container.firstChild).toHaveAttribute("aria-hidden", "true");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/product-image.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/avatar-color'`.

- [ ] **Step 3: Viết hàm thuần sinh màu**

Tạo `src/components/kit/avatar-color.ts`:

```ts
/**
 * Bam ten ra mot goc mau on dinh. Khong dung Math.random vi anh du phong
 * phai giong nhau giua server va client, neu khong React se bao hydrate lech.
 */
export function avatarHue(name: string): number {
  let hash = 0;
  for (let index = 0; index < name.length; index += 1) {
    hash = (hash * 31 + name.charCodeAt(index)) % 360;
  }
  return hash;
}

/** Chu cai dau cua toi da hai tu dau — du de phan biet trong luoi. */
export function initials(name: string): string {
  const words = name.trim().split(/\s+/).filter(Boolean);
  if (words.length === 0) return "?";
  return words
    .slice(0, 2)
    .map((word) => word[0]!.toUpperCase())
    .join("");
}
```

- [ ] **Step 4: Viết component**

Tạo `src/components/kit/product-image.tsx`:

```tsx
import Image from "next/image";

import { avatarHue, initials } from "@/components/kit/avatar-color";
import { cn } from "@/lib/utils";

interface ProductImageProps {
  src?: string | null;
  name: string;
  size?: number;
  className?: string;
}

/**
 * Phan lon san pham se chua co anh rat lau — nen anh du phong phai dep,
 * khong duoc la mot o xam trong.
 */
export function ProductImage({
  src,
  name,
  size = 64,
  className,
}: ProductImageProps) {
  if (src) {
    return (
      <Image
        src={src}
        alt={name}
        width={size}
        height={size}
        className={cn("bg-muted shrink-0 rounded-lg object-cover", className)}
      />
    );
  }

  const hue = avatarHue(name);

  return (
    <div
      aria-hidden="true"
      style={{
        width: size,
        height: size,
        backgroundColor: `oklch(0.92 0.05 ${hue})`,
        color: `oklch(0.45 0.13 ${hue})`,
      }}
      className={cn(
        "flex shrink-0 items-center justify-center rounded-lg font-semibold",
        className,
      )}
    >
      {initials(name)}
    </div>
  );
}
```

- [ ] **Step 5: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/product-image.test.tsx`
Expected: PASS — 10 test.

- [ ] **Step 6: Commit**

```bash
git add src/components/kit/avatar-color.ts src/components/kit/product-image.tsx tests/components/kit/product-image.test.tsx
git commit -m "feat(kit): add ProductImage with deterministic fallback"
```

---

## Task 7: `ProductTile` và `ResultList` — hai cách nhìn một sản phẩm

**Files:**

- Create: `src/components/kit/product-tile.tsx`
- Create: `src/components/kit/result-list.tsx`
- Test: `tests/components/kit/product-tile.test.tsx`

**Interfaces:**

- Consumes: `Money`, `StockBadge`, `ProductImage` từ các task trước; `cn` từ `@/lib/utils`
- Produces:
  - `ProductTile` — `(props: { name: string; price: number; unit: string; stock: number; imageUrl?: string | null; onSelect: () => void; className?: string }) => JSX.Element`
  - `ResultList` — `(props: { id?: string; children: React.ReactNode; className?: string }) => JSX.Element`
  - `ResultRow` — `(props: { name: string; price: number; unit: string; stock: number; imageUrl?: string | null; active: boolean; onSelect: () => void }) => JSX.Element`

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/product-tile.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";

import { ProductTile } from "@/components/kit/product-tile";
import { ResultRow } from "@/components/kit/result-list";

const product = {
  name: "Bugi Wave",
  price: 15000,
  unit: "cái",
  stock: 50,
  imageUrl: null,
};

describe("ProductTile", () => {
  it("hien ten, gia va ton kho", () => {
    render(<ProductTile {...product} onSelect={() => {}} />);
    expect(screen.getByText("Bugi Wave")).toBeInTheDocument();
    expect(screen.getByText("15.000")).toBeInTheDocument();
    expect(screen.getByText("Còn 50 cái")).toBeInTheDocument();
  });

  it("moi the deu cao bang nhau du ten dai ngan khac nhau", () => {
    const { container } = render(
      <ProductTile {...product} onSelect={() => {}} />,
    );
    expect(container.firstChild).toHaveClass("min-h-36");
  });

  it("goi onSelect khi bam", async () => {
    const onSelect = vi.fn();
    render(<ProductTile {...product} onSelect={onSelect} />);
    await userEvent.click(screen.getByRole("button"));
    expect(onSelect).toHaveBeenCalledOnce();
  });
});

describe("ResultRow", () => {
  it("dong dang chon duoc danh dau cho trinh doc man hinh", () => {
    render(<ResultRow {...product} active onSelect={() => {}} />);
    expect(screen.getByRole("option")).toHaveAttribute("aria-selected", "true");
  });

  it("dong dang chon co thanh nhan ben trai", () => {
    render(<ResultRow {...product} active onSelect={() => {}} />);
    expect(screen.getByRole("option")).toHaveClass("border-l-primary");
  });

  it("dong khong duoc chon thi thanh nhan trong suot", () => {
    render(<ResultRow {...product} active={false} onSelect={() => {}} />);
    expect(screen.getByRole("option")).toHaveClass("border-l-transparent");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/product-tile.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/product-tile'`.

- [ ] **Step 3: Viết `ProductTile`**

Tạo `src/components/kit/product-tile.tsx`:

```tsx
"use client";

import { Money } from "@/components/kit/money";
import { ProductImage } from "@/components/kit/product-image";
import { StockBadge } from "@/components/kit/stock-badge";
import { cn } from "@/lib/utils";

interface ProductTileProps {
  name: string;
  price: number;
  unit: string;
  stock: number;
  imageUrl?: string | null;
  onSelect: () => void;
  className?: string;
}

/**
 * min-h co dinh la co y: luoi hang hoa ten dai ngan khac nhau, khong ep
 * chieu cao thi cac the so le nhin rat lon xon.
 */
export function ProductTile({
  name,
  price,
  unit,
  stock,
  imageUrl,
  onSelect,
  className,
}: ProductTileProps) {
  return (
    <button
      type="button"
      onClick={onSelect}
      className={cn(
        "bg-card border-border hover:border-primary hover:bg-accent flex min-h-36 flex-col items-start gap-2 rounded-xl border p-3 text-left transition-colors",
        "focus-visible:ring-ring focus-visible:ring-[3px] focus-visible:outline-none",
        className,
      )}
    >
      <ProductImage src={imageUrl} name={name} size={48} />
      <span className="line-clamp-2 flex-1 font-medium">{name}</span>
      <div className="flex w-full items-center justify-between gap-2">
        <Money amount={price} />
        <StockBadge stock={stock} unit={unit} />
      </div>
    </button>
  );
}
```

- [ ] **Step 4: Viết `ResultList` và `ResultRow`**

Tạo `src/components/kit/result-list.tsx`:

```tsx
"use client";

import { Money } from "@/components/kit/money";
import { ProductImage } from "@/components/kit/product-image";
import { StockBadge } from "@/components/kit/stock-badge";
import { cn } from "@/lib/utils";

interface ResultListProps {
  id?: string;
  children: React.ReactNode;
  className?: string;
}

/** Khung co vien + divide-y de danh sach khong troi lan vao nen. */
export function ResultList({ id, children, className }: ResultListProps) {
  return (
    <ul
      id={id}
      role="listbox"
      className={cn(
        "border-border bg-card divide-border divide-y overflow-hidden rounded-xl border",
        className,
      )}
    >
      {children}
    </ul>
  );
}

interface ResultRowProps {
  name: string;
  price: number;
  unit: string;
  stock: number;
  imageUrl?: string | null;
  active: boolean;
  onSelect: () => void;
}

/**
 * Go rat nhanh ca ngay — dong dang chon phai khac han, nen dung ca nen dam
 * lan mot thanh nhan ben trai, khong chi doi mau nen mot chut.
 */
export function ResultRow({
  name,
  price,
  unit,
  stock,
  imageUrl,
  active,
  onSelect,
}: ResultRowProps) {
  return (
    <li
      role="option"
      aria-selected={active}
      className={cn(
        "border-l-4",
        active ? "border-l-primary bg-accent" : "border-l-transparent",
      )}
    >
      <button
        type="button"
        onClick={onSelect}
        className="min-h-touch hover:bg-accent/50 flex w-full items-center gap-3 px-4 py-3 text-left"
      >
        <ProductImage src={imageUrl} name={name} size={40} />
        <span className="flex min-w-0 flex-1 flex-col gap-1">
          <span className="truncate text-lg font-medium">{name}</span>
          <StockBadge stock={stock} unit={unit} className="w-fit" />
        </span>
        <Money amount={price} className="text-lg" />
      </button>
    </li>
  );
}
```

- [ ] **Step 5: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/product-tile.test.tsx`
Expected: PASS — 6 test.

- [ ] **Step 6: Commit**

```bash
git add src/components/kit/product-tile.tsx src/components/kit/result-list.tsx tests/components/kit/product-tile.test.tsx
git commit -m "feat(kit): add ProductTile and ResultList"
```

---

## Task 8: `SearchField` — ô tìm kiếm

**Files:**

- Create: `src/components/kit/search-field.tsx`
- Test: `tests/components/kit/search-field.test.tsx`

**Interfaces:**

- Consumes: `Input` từ `@/components/ui/input`; `cn` từ `@/lib/utils`; icon `Search`, `X` từ `lucide-react`
- Produces: `SearchField` — chuyển tiếp mọi props của `<input>`, thêm `onClear?: () => void`; dùng `React.forwardRef<HTMLInputElement>` để `/pos` giữ được `inputRef` cho phím `F2`

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/search-field.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { createRef } from "react";
import { describe, expect, it, vi } from "vitest";

import { SearchField } from "@/components/kit/search-field";

describe("SearchField", () => {
  it("chuyen tiep ref de phim tat F2 focus duoc", () => {
    const ref = createRef<HTMLInputElement>();
    render(<SearchField ref={ref} value="" onChange={() => {}} />);
    expect(ref.current).toBeInstanceOf(HTMLInputElement);
  });

  it("chua co chu thi khong hien nut xoa", () => {
    render(<SearchField value="" onChange={() => {}} onClear={() => {}} />);
    expect(
      screen.queryByRole("button", { name: "Xoá ô tìm" }),
    ).not.toBeInTheDocument();
  });

  it("co chu thi hien nut xoa va goi onClear", async () => {
    const onClear = vi.fn();
    render(<SearchField value="bugi" onChange={() => {}} onClear={onClear} />);
    await userEvent.click(screen.getByRole("button", { name: "Xoá ô tìm" }));
    expect(onClear).toHaveBeenCalledOnce();
  });

  it("cao hon o cham vi la o duoc dung nhieu nhat", () => {
    render(<SearchField value="" onChange={() => {}} />);
    expect(screen.getByRole("textbox")).toHaveClass("h-14");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/search-field.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/search-field'`.

- [ ] **Step 3: Viết component**

Tạo `src/components/kit/search-field.tsx`:

```tsx
"use client";

import { Search, X } from "lucide-react";
import { forwardRef } from "react";

import { Input } from "@/components/ui/input";
import { cn } from "@/lib/utils";

interface SearchFieldProps extends React.ComponentProps<"input"> {
  onClear?: () => void;
}

/**
 * O tim la duong vao chinh cua moi giao dich (Spec 1 muc 4) — nen no cao han
 * moi o khac tren man hinh. forwardRef vi /pos giu ref de phim F2 focus vao.
 */
export const SearchField = forwardRef<HTMLInputElement, SearchFieldProps>(
  function SearchField({ className, onClear, value, ...props }, ref) {
    const hasText = String(value ?? "").length > 0;

    return (
      <div className="relative">
        <Search
          aria-hidden="true"
          className="text-muted-foreground pointer-events-none absolute top-1/2 left-4 size-5 -translate-y-1/2"
        />
        <Input
          ref={ref}
          value={value}
          className={cn("h-14 pr-12 pl-12 text-xl", className)}
          {...props}
        />
        {hasText && onClear ? (
          <button
            type="button"
            aria-label="Xoá ô tìm"
            onClick={onClear}
            className="text-muted-foreground hover:bg-accent hover:text-foreground absolute top-1/2 right-2 flex size-9 -translate-y-1/2 items-center justify-center rounded-md"
          >
            <X className="size-5" />
          </button>
        ) : null}
      </div>
    );
  },
);
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/search-field.test.tsx`
Expected: PASS — 4 test.

- [ ] **Step 5: Commit**

```bash
git add src/components/kit/search-field.tsx tests/components/kit/search-field.test.tsx
git commit -m "feat(kit): add SearchField with icon and clear button"
```

---

## Task 9: Kit cho trang quản trị

Năm component cho `/admin/*`, gộp một task vì chúng đơn giản và luôn dùng cùng nhau.

**Files:**

- Create: `src/components/kit/page-header.tsx`
- Create: `src/components/kit/empty-state.tsx`
- Create: `src/components/kit/data-table-shell.tsx`
- Create: `src/components/kit/stat-tile.tsx`
- Create: `src/components/kit/collapsible-form-card.tsx`
- Create: `src/components/kit/index.ts`
- Test: `tests/components/kit/admin-kit.test.tsx`

**Interfaces:**

- Consumes: `Card`, `CardContent`, `CardHeader`, `CardTitle` từ `@/components/ui/card`; `Collapsible`, `CollapsibleTrigger`, `CollapsiblePanel` từ Task 2; `Money` từ Task 3; `TouchButton` từ Task 5
- Produces:
  - `PageHeader` — `(props: { title: string; description?: string; action?: React.ReactNode }) => JSX.Element`
  - `EmptyState` — `(props: { title: string; description?: string; action?: React.ReactNode; className?: string }) => JSX.Element`
  - `DataTableShell` — `(props: { title: string; count: number; isEmpty: boolean; empty: React.ReactNode; children: React.ReactNode }) => JSX.Element`
  - `StatTile` — `(props: { label: string; value: number; format?: "money" | "number"; hint?: string }) => JSX.Element`
  - `CollapsibleFormCard` — `(props: { title: string; triggerLabel: string; defaultOpen?: boolean; children: React.ReactNode }) => JSX.Element`
- Và `src/components/kit/index.ts` re-export **toàn bộ** kit từ Task 3–9.

- [ ] **Step 1: Viết test**

Tạo `tests/components/kit/admin-kit.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";

import { CollapsibleFormCard } from "@/components/kit/collapsible-form-card";
import { DataTableShell } from "@/components/kit/data-table-shell";
import { EmptyState } from "@/components/kit/empty-state";
import { PageHeader } from "@/components/kit/page-header";
import { StatTile } from "@/components/kit/stat-tile";

describe("PageHeader", () => {
  it("dat tieu de o cap do 1 cho trinh doc man hinh", () => {
    render(<PageHeader title="Sản phẩm" description="Quản lý hàng hoá" />);
    expect(
      screen.getByRole("heading", { level: 1, name: "Sản phẩm" }),
    ).toBeInTheDocument();
    expect(screen.getByText("Quản lý hàng hoá")).toBeInTheDocument();
  });
});

describe("EmptyState", () => {
  it("hien loi dan thay vi mot bang trong", () => {
    render(<EmptyState title="Chưa có sản phẩm nào" />);
    expect(screen.getByText("Chưa có sản phẩm nào")).toBeInTheDocument();
  });
});

describe("DataTableShell", () => {
  it("co du lieu thi hien bang, khong hien trang thai rong", () => {
    render(
      <DataTableShell
        title="Danh sách"
        count={2}
        isEmpty={false}
        empty={<EmptyState title="Chưa có gì" />}
      >
        <table>
          <tbody>
            <tr>
              <td>Bugi Wave</td>
            </tr>
          </tbody>
        </table>
      </DataTableShell>,
    );
    expect(screen.getByText("Danh sách (2)")).toBeInTheDocument();
    expect(screen.getByText("Bugi Wave")).toBeInTheDocument();
    expect(screen.queryByText("Chưa có gì")).not.toBeInTheDocument();
  });

  it("rong thi hien trang thai rong thay cho bang", () => {
    render(
      <DataTableShell
        title="Danh sách"
        count={0}
        isEmpty
        empty={<EmptyState title="Chưa có gì" />}
      >
        <table>
          <tbody>
            <tr>
              <td>Bugi Wave</td>
            </tr>
          </tbody>
        </table>
      </DataTableShell>,
    );
    expect(screen.getByText("Chưa có gì")).toBeInTheDocument();
    expect(screen.queryByText("Bugi Wave")).not.toBeInTheDocument();
  });
});

describe("StatTile", () => {
  it("dinh dang tien khi format la money", () => {
    render(<StatTile label="Doanh thu" value={15000} format="money" />);
    expect(screen.getByText("15.000")).toBeInTheDocument();
  });

  it("mac dinh hien so tho", () => {
    render(<StatTile label="Số đơn" value={12} />);
    expect(screen.getByText("12")).toBeInTheDocument();
  });
});

describe("CollapsibleFormCard", () => {
  it("mac dinh dong de bang khong bi day xuong duoi", () => {
    render(
      <CollapsibleFormCard title="Thêm sản phẩm" triggerLabel="Thêm sản phẩm">
        <p>Nội dung form</p>
      </CollapsibleFormCard>,
    );
    // Base UI giu panel trong DOM va chi an di, nen phai hoi "co nhin thay
    // khong" chu khong phai "co ton tai khong".
    expect(screen.queryByText("Nội dung form")).not.toBeVisible();
  });

  it("bam nut thi mo ra", async () => {
    render(
      <CollapsibleFormCard title="Thêm sản phẩm" triggerLabel="Thêm sản phẩm">
        <p>Nội dung form</p>
      </CollapsibleFormCard>,
    );
    await userEvent.click(
      screen.getByRole("button", { name: "Thêm sản phẩm" }),
    );
    expect(screen.getByText("Nội dung form")).toBeVisible();
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/admin-kit.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/collapsible-form-card'`.

- [ ] **Step 3: Viết `PageHeader` và `EmptyState`**

Tạo `src/components/kit/page-header.tsx`:

```tsx
interface PageHeaderProps {
  title: string;
  description?: string;
  action?: React.ReactNode;
}

/** Moi trang /admin mo dau giong nhau — tieu de, mot dong giai thich, mot hanh dong. */
export function PageHeader({ title, description, action }: PageHeaderProps) {
  return (
    <div className="flex flex-wrap items-start justify-between gap-4">
      <div className="space-y-1">
        <h1 className="text-2xl font-semibold tracking-tight">{title}</h1>
        {description ? (
          <p className="text-muted-foreground text-sm">{description}</p>
        ) : null}
      </div>
      {action}
    </div>
  );
}
```

Tạo `src/components/kit/empty-state.tsx`:

```tsx
import { PackageOpen } from "lucide-react";

import { cn } from "@/lib/utils";

interface EmptyStateProps {
  title: string;
  description?: string;
  action?: React.ReactNode;
  className?: string;
}

/** Bang trong khong noi len duoc dieu gi — luon thay bang mot loi dan. */
export function EmptyState({
  title,
  description,
  action,
  className,
}: EmptyStateProps) {
  return (
    <div
      className={cn(
        "flex flex-col items-center gap-3 px-6 py-12 text-center",
        className,
      )}
    >
      <PackageOpen
        aria-hidden="true"
        className="text-muted-foreground size-10"
      />
      <p className="font-medium">{title}</p>
      {description ? (
        <p className="text-muted-foreground max-w-sm text-sm">{description}</p>
      ) : null}
      {action}
    </div>
  );
}
```

- [ ] **Step 4: Viết `DataTableShell` và `StatTile`**

Tạo `src/components/kit/data-table-shell.tsx`:

```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

interface DataTableShellProps {
  title: string;
  count: number;
  isEmpty: boolean;
  empty: React.ReactNode;
  children: React.ReactNode;
}

/**
 * Vo chung cho moi bang trong /admin. Nhan isEmpty tu ben ngoai thay vi tu
 * dem children, de trang goi giu quyen quyet dinh the nao la "rong".
 */
export function DataTableShell({
  title,
  count,
  isEmpty,
  empty,
  children,
}: DataTableShellProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          {title} ({count})
        </CardTitle>
      </CardHeader>
      <CardContent className={isEmpty ? undefined : "px-0"}>
        {isEmpty ? empty : <div className="overflow-x-auto">{children}</div>}
      </CardContent>
    </Card>
  );
}
```

Tạo `src/components/kit/stat-tile.tsx`:

```tsx
import { Money } from "@/components/kit/money";
import { Card, CardContent } from "@/components/ui/card";

interface StatTileProps {
  label: string;
  value: number;
  format?: "money" | "number";
  hint?: string;
}

export function StatTile({
  label,
  value,
  format = "number",
  hint,
}: StatTileProps) {
  return (
    <Card>
      <CardContent className="space-y-1 p-4">
        <p className="text-muted-foreground text-sm">{label}</p>
        {format === "money" ? (
          <Money amount={value} className="text-2xl" />
        ) : (
          <p className="text-2xl font-semibold tabular-nums">{value}</p>
        )}
        {hint ? <p className="text-muted-foreground text-xs">{hint}</p> : null}
      </CardContent>
    </Card>
  );
}
```

- [ ] **Step 5: Viết `CollapsibleFormCard`**

Tạo `src/components/kit/collapsible-form-card.tsx`:

```tsx
"use client";

import { Plus } from "lucide-react";
import { useState } from "react";

import { TouchButton } from "@/components/kit/touch-button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import {
  Collapsible,
  CollapsiblePanel,
  CollapsibleTrigger,
} from "@/components/ui/collapsible";

interface CollapsibleFormCardProps {
  title: string;
  triggerLabel: string;
  defaultOpen?: boolean;
  children: React.ReactNode;
}

/**
 * Form them moi mac dinh dong: viec thuong lam nhat o trang danh sach la
 * *xem* danh sach, khong phai them hang. Form xoe san se day bang xuong duoi.
 */
export function CollapsibleFormCard({
  title,
  triggerLabel,
  defaultOpen = false,
  children,
}: CollapsibleFormCardProps) {
  const [open, setOpen] = useState(defaultOpen);

  return (
    <Collapsible open={open} onOpenChange={setOpen}>
      <CollapsibleTrigger
        render={
          <TouchButton variant={open ? "outline" : "default"}>
            <Plus aria-hidden="true" className="size-4" />
            {triggerLabel}
          </TouchButton>
        }
      />
      <CollapsiblePanel>
        <Card className="mt-4">
          <CardHeader>
            <CardTitle>{title}</CardTitle>
          </CardHeader>
          <CardContent>{children}</CardContent>
        </Card>
      </CollapsiblePanel>
    </Collapsible>
  );
}
```

- [ ] **Step 6: Viết barrel export**

Tạo `src/components/kit/index.ts`:

```ts
export { avatarHue, initials } from "./avatar-color";
export { ChipToggle } from "./chip-toggle";
export { CollapsibleFormCard } from "./collapsible-form-card";
export { DataTableShell } from "./data-table-shell";
export { EmptyState } from "./empty-state";
export { Money } from "./money";
export { PageHeader } from "./page-header";
export { ProductImage } from "./product-image";
export { ProductTile } from "./product-tile";
export { ResultList, ResultRow } from "./result-list";
export { SearchField } from "./search-field";
export { StatTile } from "./stat-tile";
export { LOW_STOCK_THRESHOLD, StockBadge, stockLevel } from "./stock-badge";
export type { StockLevel } from "./stock-badge";
export { TouchButton } from "./touch-button";
```

- [ ] **Step 7: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/admin-kit.test.tsx`
Expected: PASS — 8 test.

Nếu `CollapsibleTrigger` không nhận prop `render` (khác bản Base UI), thay
Step 5 bằng `<CollapsibleTrigger asChild>` bọc `TouchButton`, rồi chạy lại.

- [ ] **Step 8: Commit**

```bash
git add src/components/kit tests/components/kit/admin-kit.test.tsx
git commit -m "feat(kit): add admin presentation components and barrel export"
```

---

## Task 10: Trang gallery `/dev/kit`

Đây là chỗ "sau chỉ lấy ra dùng thôi", và cũng là lưới an toàn khi đổi token màu.

**Files:**

- Create: `src/app/dev/kit/page.tsx`
- Test: `tests/components/kit/gallery-guard.test.ts`

**Interfaces:**

- Consumes: toàn bộ `@/components/kit`
- Produces: route `/dev/kit`; hàm `isGalleryEnabled(nodeEnv: string): boolean`

- [ ] **Step 1: Viết test cho hàm chặn production**

Tạo `tests/components/kit/gallery-guard.test.ts`:

```ts
import { describe, expect, it } from "vitest";

import { isGalleryEnabled } from "@/app/dev/kit/page";

describe("isGalleryEnabled", () => {
  it("bat o dev va test", () => {
    expect(isGalleryEnabled("development")).toBe(true);
    expect(isGalleryEnabled("test")).toBe(true);
  });

  it("tat o production — trang noi bo khong duoc lo ra ngoai", () => {
    expect(isGalleryEnabled("production")).toBe(false);
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/gallery-guard.test.ts`
Expected: FAIL — `Cannot find module '@/app/dev/kit/page'`.

- [ ] **Step 3: Viết trang gallery**

Tạo `src/app/dev/kit/page.tsx`:

```tsx
import { notFound } from "next/navigation";

import {
  ChipToggle,
  CollapsibleFormCard,
  DataTableShell,
  EmptyState,
  Money,
  PageHeader,
  ProductImage,
  ProductTile,
  ResultList,
  ResultRow,
  SearchField,
  StatTile,
  StockBadge,
  TouchButton,
} from "@/components/kit";

export function isGalleryEnabled(nodeEnv: string): boolean {
  return nodeEnv !== "production";
}

const SAMPLE = [
  { name: "Bugi Wave", price: 15000, unit: "cái", stock: 50, imageUrl: null },
  {
    name: "Nhớt Castrol 4T",
    price: 95000,
    unit: "chai",
    stock: 3,
    imageUrl: null,
  },
  { name: "Dây điện", price: 12000, unit: "mét", stock: 0, imageUrl: null },
];

function Section({
  title,
  children,
}: {
  title: string;
  children: React.ReactNode;
}) {
  return (
    <section className="space-y-3">
      <h2 className="border-border border-b pb-1 text-lg font-semibold">
        {title}
      </h2>
      {children}
    </section>
  );
}

/**
 * Trang xem toan bo kit. Khong phai giao dien nguoi dung — day la cho de
 * kiem mat moi component o ca hai theme truoc khi tin vao chung.
 */
export default function KitGalleryPage() {
  if (!isGalleryEnabled(process.env.NODE_ENV)) notFound();

  return (
    <div className="mx-auto max-w-5xl space-y-10 p-8">
      <PageHeader
        title="UI Kit"
        description="Đổi theme của trình duyệt để xem cả light lẫn dark."
        action={<TouchButton>Hành động chính</TouchButton>}
      />

      <Section title="Money">
        <div className="flex items-baseline gap-6">
          <Money amount={15000} size="sm" />
          <Money amount={150000} />
          <Money amount={1500000} size="display" />
        </div>
      </Section>

      <Section title="StockBadge">
        <div className="flex flex-wrap gap-2">
          <StockBadge stock={50} unit="cái" />
          <StockBadge stock={2.5} unit="mét" />
          <StockBadge stock={0} unit="cái" />
          <StockBadge stock={-3} unit="cái" />
        </div>
      </Section>

      <Section title="ChipToggle">
        <div className="flex flex-wrap gap-2">
          <ChipToggle label="Tạp hoá" selected onToggle={() => {}} />
          <ChipToggle label="Phụ tùng" selected={false} onToggle={() => {}} />
          <ChipToggle label="Nước ngọt" selected={false} onToggle={() => {}} />
        </div>
      </Section>

      <Section title="ProductImage">
        <div className="flex items-end gap-3">
          <ProductImage src={null} name="Bugi Wave" size={40} />
          <ProductImage src={null} name="Nhớt Castrol" size={64} />
          <ProductImage src={null} name="Dây điện" size={96} />
        </div>
      </Section>

      <Section title="ProductTile">
        <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">
          {SAMPLE.map((product) => (
            <ProductTile key={product.name} {...product} onSelect={() => {}} />
          ))}
        </div>
      </Section>

      <Section title="SearchField">
        <div className="space-y-3">
          <SearchField
            value=""
            readOnly
            placeholder="Tìm sản phẩm... (F2)"
            onChange={() => {}}
          />
          <SearchField
            value="bugi"
            readOnly
            onChange={() => {}}
            onClear={() => {}}
          />
        </div>
      </Section>

      <Section title="ResultList">
        <ResultList>
          {SAMPLE.map((product, index) => (
            <ResultRow
              key={product.name}
              {...product}
              active={index === 0}
              onSelect={() => {}}
            />
          ))}
        </ResultList>
      </Section>

      <Section title="StatTile">
        <div className="grid grid-cols-2 gap-3 sm:grid-cols-4">
          <StatTile label="Doanh thu" value={4250000} format="money" />
          <StatTile label="Số đơn" value={37} hint="Hôm nay" />
        </div>
      </Section>

      <Section title="EmptyState">
        <EmptyState
          title="Chưa có sản phẩm nào"
          description="Thêm sản phẩm đầu tiên để bắt đầu bán hàng."
          action={<TouchButton>Thêm sản phẩm</TouchButton>}
        />
      </Section>

      <Section title="DataTableShell">
        <DataTableShell
          title="Danh sách"
          count={0}
          isEmpty
          empty={<EmptyState title="Chưa có sản phẩm nào" />}
        >
          <table />
        </DataTableShell>
      </Section>

      <Section title="CollapsibleFormCard">
        <CollapsibleFormCard title="Thêm sản phẩm" triggerLabel="Thêm sản phẩm">
          <p className="text-muted-foreground text-sm">Nội dung form ở đây.</p>
        </CollapsibleFormCard>
      </Section>
    </div>
  );
}
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/gallery-guard.test.ts`
Expected: PASS — 2 test.

- [ ] **Step 5: Xem bằng mắt**

Run: `pnpm dev`, mở `http://localhost:3000/dev/kit`.
Expected: mọi component hiện đúng, không có ô xám trống, badge tồn kho ba màu
khác nhau rõ rệt. Đổi theme hệ điều hành sang dark rồi xem lại — chữ vẫn đọc
được trên mọi nền.

- [ ] **Step 6: Commit**

```bash
git add src/app/dev/kit/page.tsx tests/components/kit/gallery-guard.test.ts
git commit -m "feat(kit): add /dev/kit gallery page"
```

---

## Task 11: `save-image` — nhận, kiểm, resize, ghi ảnh

**Files:**

- Modify: `package.json` (thêm `sharp`)
- Modify: `pnpm-workspace.yaml`
- Modify: `.gitignore`
- Create: `src/server/products/save-image.ts`
- Test: `tests/server/products/save-image.test.ts`

**Interfaces:**

- Consumes: `sharp`; `node:fs/promises`; `node:path`
- Produces:
  - `MAX_IMAGE_BYTES = 5 * 1024 * 1024`
  - `ALLOWED_IMAGE_TYPES = ["image/jpeg", "image/png", "image/webp"]`
  - `IMAGE_SIZE = 300`
  - `saveProductImage(file: File): Promise<{ ok: true; imageUrl: string } | { ok: false; message: string }>`
  - `deleteProductImage(imageUrl: string | null | undefined): Promise<void>`

- [ ] **Step 1: Thêm dependency**

Run: `pnpm add sharp`

Rồi sửa `pnpm-workspace.yaml` — `sharp` đang bị chặn build script, phải cho
phép, nếu không binary native không được biên dịch và `sharp` sẽ ném lỗi lúc
chạy. Xoá `sharp` khỏi `ignoredBuiltDependencies` và thêm vào
`onlyBuiltDependencies`:

```yaml
ignoredBuiltDependencies:
  - unrs-resolver

onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - esbuild
  - sharp
```

Run: `pnpm install`
Expected: cài lại và biên dịch binary của `sharp`.

- [ ] **Step 2: Bỏ qua thư mục ảnh trong git**

Thêm vào cuối `.gitignore`:

```
# anh san pham nguoi dung upload luc chay
/public/uploads/
```

- [ ] **Step 3: Viết test**

Tạo `tests/server/products/save-image.test.ts`:

```ts
import { readFile, rm } from "node:fs/promises";
import path from "node:path";

import sharp from "sharp";
import { afterAll, describe, expect, it } from "vitest";

import {
  IMAGE_SIZE,
  MAX_IMAGE_BYTES,
  deleteProductImage,
  saveProductImage,
} from "@/server/products/save-image";

const UPLOAD_DIR = path.resolve(process.cwd(), "public/uploads");
const written: string[] = [];

async function pngFile(name = "anh.png"): Promise<File> {
  const buffer = await sharp({
    create: {
      width: 800,
      height: 600,
      channels: 3,
      background: { r: 200, g: 40, b: 40 },
    },
  })
    .png()
    .toBuffer();

  return new File([new Uint8Array(buffer)], name, { type: "image/png" });
}

afterAll(async () => {
  for (const file of written) {
    await rm(path.join(UPLOAD_DIR, path.basename(file)), { force: true });
  }
});

describe("saveProductImage", () => {
  it("tu choi kieu file khong phai anh", async () => {
    const file = new File([new Uint8Array([1, 2, 3])], "x.pdf", {
      type: "application/pdf",
    });
    const result = await saveProductImage(file);
    expect(result.ok).toBe(false);
    if (!result.ok) expect(result.message).toContain("định dạng");
  });

  it("tu choi file qua co", async () => {
    const big = new Uint8Array(MAX_IMAGE_BYTES + 1);
    const file = new File([big], "to.png", { type: "image/png" });
    const result = await saveProductImage(file);
    expect(result.ok).toBe(false);
    if (!result.ok) expect(result.message).toContain("5MB");
  });

  it("luu anh hop le thanh webp vuong dung kich thuoc", async () => {
    const result = await saveProductImage(await pngFile());
    expect(result.ok).toBe(true);
    if (!result.ok) return;

    written.push(result.imageUrl);
    expect(result.imageUrl).toMatch(/^\/uploads\/[a-z0-9]+\.webp$/);

    const bytes = await readFile(
      path.join(UPLOAD_DIR, path.basename(result.imageUrl)),
    );
    const meta = await sharp(bytes).metadata();
    expect(meta.format).toBe("webp");
    expect(meta.width).toBe(IMAGE_SIZE);
    expect(meta.height).toBe(IMAGE_SIZE);
  });

  it("hai lan luu ra hai duong dan khac nhau", async () => {
    const first = await saveProductImage(await pngFile());
    const second = await saveProductImage(await pngFile());
    expect(first.ok && second.ok).toBe(true);
    if (first.ok) written.push(first.imageUrl);
    if (second.ok) written.push(second.imageUrl);
    if (first.ok && second.ok) {
      expect(first.imageUrl).not.toBe(second.imageUrl);
    }
  });
});

describe("deleteProductImage", () => {
  it("xoa duoc anh da luu", async () => {
    const saved = await saveProductImage(await pngFile());
    expect(saved.ok).toBe(true);
    if (!saved.ok) return;

    await deleteProductImage(saved.imageUrl);
    await expect(
      readFile(path.join(UPLOAD_DIR, path.basename(saved.imageUrl))),
    ).rejects.toThrow();
  });

  it("bo qua duong dan rong", async () => {
    await expect(deleteProductImage(null)).resolves.toBeUndefined();
  });

  it("khong xoa file ngoai thu muc uploads", async () => {
    await expect(
      deleteProductImage("/uploads/../../package.json"),
    ).resolves.toBeUndefined();
    await expect(readFile("package.json")).resolves.toBeTruthy();
  });
});
```

- [ ] **Step 4: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/server/products/save-image.test.ts`
Expected: FAIL — `Cannot find module '@/server/products/save-image'`.

- [ ] **Step 5: Viết module**

Tạo `src/server/products/save-image.ts`:

```ts
import { mkdir, rm, writeFile } from "node:fs/promises";
import path from "node:path";

import { createId } from "@paralleldrive/cuid2";
import sharp from "sharp";

export const MAX_IMAGE_BYTES = 5 * 1024 * 1024;
export const ALLOWED_IMAGE_TYPES = ["image/jpeg", "image/png", "image/webp"];
/** Anh chi dung lam thumbnail trong luoi va bang — 300px la du. */
export const IMAGE_SIZE = 300;

const UPLOAD_DIR = path.resolve(process.cwd(), "public/uploads");
const PUBLIC_PREFIX = "/uploads/";

export type SaveImageResult =
  | { ok: true; imageUrl: string }
  | { ok: false; message: string };

/**
 * Resize truoc khi ghi la co y: catalog duoc nap TOAN BO mot lan luc mo ca
 * (Spec 1 muc 4), nen anh goc tu dien thoai se lam quay cham hang chuc giay.
 */
export async function saveProductImage(file: File): Promise<SaveImageResult> {
  if (!ALLOWED_IMAGE_TYPES.includes(file.type)) {
    return {
      ok: false,
      message: "Ảnh phải ở định dạng JPG, PNG hoặc WEBP",
    };
  }

  if (file.size > MAX_IMAGE_BYTES) {
    return { ok: false, message: "Ảnh không được lớn hơn 5MB" };
  }

  const input = Buffer.from(await file.arrayBuffer());

  let output: Buffer;
  try {
    output = await sharp(input)
      .resize(IMAGE_SIZE, IMAGE_SIZE, { fit: "cover" })
      .webp({ quality: 80 })
      .toBuffer();
  } catch {
    return { ok: false, message: "Không đọc được file ảnh" };
  }

  await mkdir(UPLOAD_DIR, { recursive: true });

  const name = `${createId()}.webp`;
  await writeFile(path.join(UPLOAD_DIR, name), output);

  return { ok: true, imageUrl: `${PUBLIC_PREFIX}${name}` };
}

/**
 * Thay anh nghia la ghi file moi — file cu phai duoc don, neu khong thu muc
 * uploads se phinh mai. Chi xoa trong dung thu muc uploads.
 */
export async function deleteProductImage(
  imageUrl: string | null | undefined,
): Promise<void> {
  if (!imageUrl || !imageUrl.startsWith(PUBLIC_PREFIX)) return;

  const target = path.resolve(UPLOAD_DIR, path.basename(imageUrl));
  if (path.dirname(target) !== UPLOAD_DIR) return;

  await rm(target, { force: true });
}
```

Nếu `@paralleldrive/cuid2` chưa có trong `package.json`, chạy
`pnpm add @paralleldrive/cuid2` — Prisma sinh cuid ở tầng DB nên gói này chưa
chắc đã được cài như dependency trực tiếp.

- [ ] **Step 6: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/server/products/save-image.test.ts`
Expected: PASS — 7 test.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-lock.yaml pnpm-workspace.yaml .gitignore src/server/products/save-image.ts tests/server/products/save-image.test.ts
git commit -m "feat(products): resize and store uploaded product images"
```

---

## Task 12: Đưa `imageUrl` chạy suốt từ DB ra POS

`Product.imageUrl` đã có trong schema từ Plan 1 nhưng chưa ai đọc nó.

**Files:**

- Modify: `src/lib/search/types.ts`
- Modify: `src/app/api/catalog/route.ts:20-30`
- Modify: `src/server/products/save-product.ts`
- Modify: `prisma/seed.ts`
- Test: `tests/server/products/save-product.test.ts` (đã tồn tại — thêm test)

**Interfaces:**

- Consumes: `saveProduct` từ `@/server/products/save-product`
- Produces: `SearchableProduct.imageUrl: string | null`; `SaveProductInput.imageUrl?: string | null`

- [ ] **Step 1: Viết test cho `saveProduct`**

Thêm vào cuối `tests/server/products/save-product.test.ts`, bên trong khối
`describe` ngoài cùng (nếu file dùng nhiều `describe`, thêm một khối mới ở
cấp cao nhất):

```ts
it("luu duoc duong dan anh", async () => {
  const { id } = await saveProduct({
    name: "Bugi Wave",
    unit: "cái",
    price: 15000,
    costPrice: 10000,
    stock: 10,
    isActive: true,
    imageUrl: "/uploads/abc.webp",
  });

  const saved = await prisma.product.findUniqueOrThrow({ where: { id } });
  expect(saved.imageUrl).toBe("/uploads/abc.webp");
});

it("khong truyen imageUrl thi khong xoa mat anh dang co", async () => {
  const { id } = await saveProduct({
    name: "Bugi Wave",
    unit: "cái",
    price: 15000,
    costPrice: 10000,
    stock: 10,
    isActive: true,
    imageUrl: "/uploads/abc.webp",
  });

  await saveProduct({
    id,
    name: "Bugi Wave 110",
    unit: "cái",
    price: 16000,
    costPrice: 10000,
    stock: 10,
    isActive: true,
  });

  const saved = await prisma.product.findUniqueOrThrow({ where: { id } });
  expect(saved.imageUrl).toBe("/uploads/abc.webp");
});
```

Nếu file chưa import `prisma`, thêm `import { prisma } from "@/server/db/prisma";`.

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/server/products/save-product.test.ts`
Expected: FAIL — TypeScript báo `imageUrl` không có trong `SaveProductInput`.

- [ ] **Step 3: Cho `saveProduct` nhận `imageUrl`**

Trong `src/server/products/save-product.ts`, thêm vào `SaveProductInput` ngay
sau dòng `aliases?: string | null;`:

```ts
  /** Bo qua (undefined) nghia la giu nguyen anh dang co. */
  imageUrl?: string | null;
```

Rồi trong hàm `saveProduct`, sau khi dựng `data`, thêm ngay trước khối `if (input.id)`:

```ts
const dataWithImage =
  input.imageUrl === undefined ? data : { ...data, imageUrl: input.imageUrl };
```

Và đổi hai lời gọi Prisma để dùng `dataWithImage` thay cho `data`:

```ts
if (input.id) {
  const updated = await prisma.product.update({
    where: { id: input.id },
    data: dataWithImage,
    select: { id: true },
  });
  return updated;
}

const created = await prisma.product.create({
  data: dataWithImage,
  select: { id: true },
});
return created;
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/server/products/save-product.test.ts`
Expected: PASS.

- [ ] **Step 5: Thêm `imageUrl` vào kiểu dùng chung**

Trong `src/lib/search/types.ts`, thêm vào `SearchableProduct` sau dòng `soldCount: number;`:

```ts
imageUrl: string | null;
```

- [ ] **Step 6: Trả `imageUrl` trong catalog**

Trong `src/app/api/catalog/route.ts`, thêm vào khối `select` của
`prisma.product.findMany`, sau dòng `soldCount: true,`:

```ts
        imageUrl: true,
```

- [ ] **Step 7: Sửa mọi chỗ dựng `SearchableProduct` bị vỡ kiểu**

Run: `pnpm typecheck`
Expected: báo lỗi ở các file dựng `SearchableProduct` bằng tay — thường là
`prisma/seed.ts`, `src/app/pos/page.tsx`, và các file test trong
`tests/lib/search/`. Thêm `imageUrl: null` vào mỗi chỗ dựng object, và
`imageUrl: true` vào mỗi `select` của Prisma trả về `SearchableProduct`.
Chạy lại `pnpm typecheck` đến khi sạch.

- [ ] **Step 8: Cho vài sản phẩm mẫu có ảnh**

Trong `prisma/seed.ts`, không thêm file ảnh thật (vì `public/uploads/` đã bị
gitignore và seed phải chạy được trên máy trắng). Để `imageUrl: null` cho mọi
sản phẩm mẫu — ảnh dự phòng của `ProductImage` sẽ lo phần nhìn.

Ghi chú này thay cho việc thêm ảnh mẫu; không có thay đổi code ở bước này
ngoài `imageUrl: null` đã thêm ở Step 7.

- [ ] **Step 9: Chạy toàn bộ test**

Run: `pnpm vitest run`
Expected: PASS toàn bộ.

- [ ] **Step 10: Commit**

```bash
git add src/lib/search/types.ts src/app/api/catalog/route.ts src/server/products/save-product.ts prisma/seed.ts src/app/pos/page.tsx tests/
git commit -m "feat(products): carry imageUrl from database through to POS"
```

---

## Task 13: Ô chọn ảnh trong form admin

**Files:**

- Modify: `src/app/admin/products/actions.ts`
- Modify: `src/app/admin/products/product-form.tsx`
- Test: `tests/components/kit/image-picker.test.tsx`
- Create: `src/components/kit/image-picker.tsx`

**Interfaces:**

- Consumes: `ProductImage` từ Task 6; `saveProductImage` từ Task 11
- Produces: `ImagePicker` — `(props: { name: string; currentUrl?: string | null; productName: string }) => JSX.Element`; `saveProductAction` nhận thêm field `image` trong `FormData`

- [ ] **Step 1: Viết test cho `ImagePicker`**

Tạo `tests/components/kit/image-picker.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it } from "vitest";

import { ImagePicker } from "@/components/kit/image-picker";

describe("ImagePicker", () => {
  it("chua chon gi thi hien anh du phong theo ten san pham", () => {
    render(<ImagePicker name="image" productName="Bugi Wave" />);
    expect(screen.getByText("BW")).toBeInTheDocument();
  });

  it("co anh cu thi hien anh cu", () => {
    render(
      <ImagePicker
        name="image"
        productName="Bugi Wave"
        currentUrl="/uploads/abc.webp"
      />,
    );
    expect(screen.getByRole("img", { name: "Bugi Wave" })).toHaveAttribute(
      "src",
      "/uploads/abc.webp",
    );
  });

  it("o file mang dung ten de Server Action doc duoc", () => {
    const { container } = render(
      <ImagePicker name="image" productName="Bugi Wave" />,
    );
    const input = container.querySelector('input[type="file"]');
    expect(input).toHaveAttribute("name", "image");
    expect(input).toHaveAttribute("accept", "image/jpeg,image/png,image/webp");
  });

  it("chon anh xong thi hien anh xem truoc thay cho anh du phong", async () => {
    const { container } = render(
      <ImagePicker name="image" productName="Bugi Wave" />,
    );
    const input =
      container.querySelector<HTMLInputElement>('input[type="file"]')!;

    const file = new File([new Uint8Array([1, 2, 3])], "anh.png", {
      type: "image/png",
    });
    await userEvent.upload(input, file);

    expect(
      await screen.findByRole("img", { name: "Xem trước" }),
    ).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/components/kit/image-picker.test.tsx`
Expected: FAIL — `Cannot find module '@/components/kit/image-picker'`.

- [ ] **Step 3: Viết `ImagePicker`**

Tạo `src/components/kit/image-picker.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";

import { ProductImage } from "@/components/kit/product-image";
import { Input } from "@/components/ui/input";

interface ImagePickerProps {
  name: string;
  productName: string;
  currentUrl?: string | null;
}

/**
 * Xem truoc bang object URL ngay tren may — chu quan thay minh chon dung anh
 * chua truoc khi bam luu, khong phai luu roi moi biet.
 */
export function ImagePicker({
  name,
  productName,
  currentUrl,
}: ImagePickerProps) {
  const [preview, setPreview] = useState<string | null>(null);

  useEffect(() => {
    // Object URL giu bo nho cho den khi duoc thu hoi.
    return () => {
      if (preview) URL.revokeObjectURL(preview);
    };
  }, [preview]);

  function handleChange(event: React.ChangeEvent<HTMLInputElement>) {
    const file = event.target.files?.[0];
    setPreview((old) => {
      if (old) URL.revokeObjectURL(old);
      return file ? URL.createObjectURL(file) : null;
    });
  }

  return (
    <div className="flex items-center gap-4">
      {preview ? (
        // eslint-disable-next-line @next/next/no-img-element -- object URL, khong qua next/image duoc
        <img
          src={preview}
          alt="Xem trước"
          width={64}
          height={64}
          className="size-16 shrink-0 rounded-lg object-cover"
        />
      ) : (
        <ProductImage src={currentUrl} name={productName} size={64} />
      )}

      <Input
        type="file"
        name={name}
        accept="image/jpeg,image/png,image/webp"
        onChange={handleChange}
        className="max-w-xs"
      />
    </div>
  );
}
```

Thêm vào `src/components/kit/index.ts`, giữ đúng thứ tự chữ cái:

```ts
export { ImagePicker } from "./image-picker";
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/components/kit/image-picker.test.tsx`
Expected: PASS — 4 test.

- [ ] **Step 5: Cho Server Action nhận file**

Trong `src/app/admin/products/actions.ts`, thêm import:

```ts
import { saveProductImage } from "@/server/products/save-image";
```

Rồi trong `saveProductAction`, chèn khối sau ngay **sau** kiểm tra
`if (!parsed.success)` và **trước** lời gọi `saveProduct`:

```ts
const image = formData.get("image");
let imageUrl: string | undefined;

if (image instanceof File && image.size > 0) {
  const saved = await saveProductImage(image);
  if (!saved.ok) {
    return { ok: false as const, message: saved.message };
  }
  imageUrl = saved.imageUrl;
}
```

Và đổi lời gọi `saveProduct` thành:

```ts
await saveProduct({ ...parsed.data, isActive: true, imageUrl });
```

Lưu ý: `imageUrl` là `undefined` khi người dùng không chọn ảnh, và Task 12
Step 3 đã làm `saveProduct` hiểu `undefined` là "giữ nguyên ảnh đang có".

- [ ] **Step 6: Gắn `ImagePicker` vào form**

Trong `src/app/admin/products/product-form.tsx`, thêm import:

```tsx
import { ImagePicker } from "@/components/kit/image-picker";
import { Label } from "@/components/ui/label";
```

(`Label` đã được import sẵn — không thêm trùng.)

Rồi thêm khối sau vào trong `<form>`, ngay trước khối `col-span-2` chứa nút submit:

```tsx
<div className="col-span-2 space-y-1.5">
  <Label htmlFor="product-image">Ảnh sản phẩm</Label>
  <ImagePicker name="image" productName="Sản phẩm" currentUrl={null} />
</div>
```

- [ ] **Step 7: Thêm `ImagePicker` vào gallery**

`/dev/kit` phải xem được mọi component của kit. Trong `src/app/dev/kit/page.tsx`,
thêm `ImagePicker` vào khối import từ `@/components/kit`, rồi thêm section sau
ngay trước `<Section title="StatTile">`:

```tsx
<Section title="ImagePicker">
  <ImagePicker name="demo-image" productName="Bugi Wave" />
</Section>
```

- [ ] **Step 8: Kiểm bằng tay**

Run: `pnpm dev`, mở `http://localhost:3000/admin/products`, chọn một ảnh JPG.
Expected: ảnh xem trước hiện ngay; bấm "Thêm sản phẩm" thì lưu thành công, và
file `.webp` xuất hiện trong `public/uploads/`.

Thử chọn một file PDF đổi đuôi thành `.png`.
Expected: báo "Ảnh phải ở định dạng JPG, PNG hoặc WEBP" hoặc "Không đọc được
file ảnh", và không có file nào được ghi.

- [ ] **Step 9: Commit**

```bash
git add src/components/kit/image-picker.tsx src/components/kit/index.ts src/app/dev/kit/page.tsx src/app/admin/products tests/components/kit/image-picker.test.tsx
git commit -m "feat(admin): upload a product image from the product form"
```

---

## Task 14: Service worker cache ảnh

Không có bước này thì mất mạng là vỡ ảnh, dù ảnh nằm cùng server.

**Files:**

- Modify: `public/sw.js`
- Test: `tests/lib/sync/sw-routing.test.ts`

**Interfaces:**

- Consumes: không có
- Produces: `public/sw.js` xử lý `/uploads/` bằng cache-first, cache riêng tên `pos-images-v1`

- [ ] **Step 1: Viết test cho luật định tuyến**

Service worker khó test trực tiếp trong jsdom, nên tách luật định tuyến thành
một hàm thuần và test hàm đó. Tạo `tests/lib/sync/sw-routing.test.ts`:

```ts
import { readFileSync } from "node:fs";
import path from "node:path";

import { describe, expect, it } from "vitest";

const source = readFileSync(
  path.resolve(__dirname, "../../../public/sw.js"),
  "utf8",
);

/** Nap ham routeFor tu sw.js ma khong can moi truong service worker. */
function loadRouteFor(): (pathname: string) => string {
  const start = source.indexOf("function routeFor");
  if (start === -1) throw new Error("sw.js phai export mot ham routeFor");
  const end = source.indexOf("\n}", start) + 2;
  const factory = new Function(`${source.slice(start, end)}; return routeFor;`);
  return factory() as (pathname: string) => string;
}

describe("routeFor", () => {
  const routeFor = loadRouteFor();

  it("man hinh ban di duong network-first", () => {
    expect(routeFor("/pos")).toBe("shell");
    expect(routeFor("/pos/anything")).toBe("shell");
  });

  it("anh san pham di duong cache-first — anh bat bien nen khong can hoi lai", () => {
    expect(routeFor("/uploads/abc.webp")).toBe("image");
  });

  it("KHONG dung den /api — hang doi IndexedDB lo phan offline", () => {
    expect(routeFor("/api/orders")).toBe("skip");
    expect(routeFor("/api/catalog")).toBe("skip");
  });

  it("KHONG dung den /admin — spec da chot la khong offline", () => {
    expect(routeFor("/admin")).toBe("skip");
    expect(routeFor("/admin/products")).toBe("skip");
  });

  it("moi duong khac deu bo qua", () => {
    expect(routeFor("/login")).toBe("skip");
    expect(routeFor("/")).toBe("skip");
  });
});
```

- [ ] **Step 2: Chạy test để chắc nó fail**

Run: `pnpm vitest run tests/lib/sync/sw-routing.test.ts`
Expected: FAIL — "sw.js phai export mot ham routeFor".

- [ ] **Step 3: Viết lại `public/sw.js`**

Thay toàn bộ nội dung `public/sw.js`:

```js
const CACHE_NAME = "pos-shell-v1";
const IMAGE_CACHE = "pos-images-v1";
const SHELL_URLS = ["/pos"];

/**
 * Mot cho duy nhat quyet dinh moi duong dan di duong nao. Tach ra thanh ham
 * thuan de test duoc ma khong can moi truong service worker.
 */
function routeFor(pathname) {
  if (pathname.startsWith("/api/")) return "skip";
  if (pathname.startsWith("/admin")) return "skip";
  if (pathname.startsWith("/uploads/")) return "image";
  if (pathname.startsWith("/pos")) return "shell";
  return "skip";
}

self.addEventListener("install", (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => cache.addAll(SHELL_URLS)),
  );
  self.skipWaiting();
});

self.addEventListener("activate", (event) => {
  const keep = [CACHE_NAME, IMAGE_CACHE];
  event.waitUntil(
    caches
      .keys()
      .then((keys) =>
        Promise.all(
          keys
            .filter((key) => !keep.includes(key))
            .map((key) => caches.delete(key)),
        ),
      ),
  );
  self.clients.claim();
});

/** Network-first: man hinh ban phai moi, nhung mat mang thi lay ban cache. */
function shellStrategy(request) {
  return fetch(request)
    .then((response) => {
      const copy = response.clone();
      void caches.open(CACHE_NAME).then((cache) => cache.put(request, copy));
      return response;
    })
    .catch(() =>
      caches.match(request).then((cached) => cached ?? Response.error()),
    );
}

/**
 * Cache-first: anh bat bien — ten file theo cuid, sua anh nghia la file moi.
 * Da co trong cache thi khong bao gio can hoi lai mang.
 */
function imageStrategy(request) {
  return caches.match(request).then((cached) => {
    if (cached) return cached;

    return fetch(request)
      .then((response) => {
        if (response.ok) {
          const copy = response.clone();
          void caches
            .open(IMAGE_CACHE)
            .then((cache) => cache.put(request, copy));
        }
        return response;
      })
      .catch(() => Response.error());
  });
}

self.addEventListener("fetch", (event) => {
  if (event.request.method !== "GET") return;

  const route = routeFor(new URL(event.request.url).pathname);
  if (route === "skip") return;

  event.respondWith(
    route === "image"
      ? imageStrategy(event.request)
      : shellStrategy(event.request),
  );
});
```

- [ ] **Step 4: Chạy test để chắc nó pass**

Run: `pnpm vitest run tests/lib/sync/sw-routing.test.ts`
Expected: PASS — 8 test.

- [ ] **Step 5: Commit**

```bash
git add public/sw.js tests/lib/sync/sw-routing.test.ts
git commit -m "feat(pos): cache product images for offline use"
```

---

## Task 15: Áp kit vào màn bán hàng

Đợt 1, phần POS. Giữ nguyên bố cục 2 cột và mọi hành vi bàn phím.

**Files:**

- Modify: `src/components/pos/product-search.tsx`
- Modify: `src/components/pos/category-grid.tsx`
- Test: `tests/components/pos/product-search.test.tsx` (đã tồn tại)

**Interfaces:**

- Consumes: `SearchField`, `ResultList`, `ResultRow`, `ChipToggle`, `ProductTile`, `EmptyState` từ `@/components/kit`
- Produces: không đổi props công khai của `ProductSearch` và `CategoryGrid`

- [ ] **Step 1: Chạy test hiện có để có mốc so sánh**

Run: `pnpm vitest run tests/components/pos`
Expected: PASS. Ghi lại số test — sau khi sửa phải vẫn đúng bằng đó và vẫn xanh.

- [ ] **Step 2: Viết lại `ProductSearch` dùng kit**

Thay toàn bộ phần `return` của `src/components/pos/product-search.tsx` (từ
`return (` đến hết hàm) bằng:

```tsx
  return (
    <div className="flex flex-col gap-3">
      <SearchField
        ref={inputRef}
        role="combobox"
        aria-expanded={results.length > 0}
        aria-controls="pos-search-results"
        value={query}
        autoFocus
        placeholder="Tìm sản phẩm... (F2)"
        onChange={(event) => {
          setQuery(event.target.value);
          setActiveIndex(0);
        }}
        onKeyDown={handleKeyDown}
        onClear={reset}
      />

      {showEmpty ? (
        <EmptyState
          title="Không tìm thấy sản phẩm nào"
          description="Thử gõ ít chữ hơn, hoặc chọn theo danh mục bên dưới."
        />
      ) : null}

      {results.length > 0 ? (
        <ResultList id="pos-search-results">
          {results.map((product, index) => (
            <ResultRow
              key={product.id}
              name={product.name}
              price={product.price}
              unit={product.unit}
              stock={product.stock}
              imageUrl={product.imageUrl}
              active={index === activeIndex}
              onSelect={() => choose(product)}
            />
          ))}
        </ResultList>
      ) : null}
    </div>
  );
}
```

Và thay khối import — bỏ `Input`, `formatVnd`, `cn` nếu không còn dùng:

```tsx
import {
  EmptyState,
  ResultList,
  ResultRow,
  SearchField,
} from "@/components/kit";
import { searchProducts } from "@/lib/search/match";
import type { SearchableProduct } from "@/lib/search/types";
```

Giữ nguyên toàn bộ `useState`, `useMemo`, `reset`, `choose`, `handleKeyDown` —
phím `↑` `↓` `Enter` `Esc` phải hoạt động y như cũ.

- [ ] **Step 3: Viết lại `CategoryGrid` dùng kit**

Thay toàn bộ phần `return` của `src/components/pos/category-grid.tsx` bằng:

```tsx
  return (
    <div className="flex flex-col gap-3">
      <div className="flex flex-wrap gap-2">
        {categories.map((category) => (
          <ChipToggle
            key={category.id}
            label={category.name}
            selected={activeCategoryId === category.id}
            onToggle={() =>
              onCategoryChange(
                activeCategoryId === category.id ? null : category.id,
              )
            }
          />
        ))}
      </div>

      <div className="grid grid-cols-2 gap-3 sm:grid-cols-3">
        {visible.map((product) => (
          <ProductTile
            key={product.id}
            name={product.name}
            price={product.price}
            unit={product.unit}
            stock={product.stock}
            imageUrl={product.imageUrl}
            onSelect={() => onSelect(product)}
          />
        ))}
      </div>
    </div>
  );
}
```

Và thay khối import:

```tsx
import { ChipToggle, ProductTile } from "@/components/kit";
import type { SearchableProduct } from "@/lib/search/types";
import type { CatalogCategory } from "@/types/catalog";
```

- [ ] **Step 4: Chạy test POS**

Run: `pnpm vitest run tests/components/pos`
Expected: PASS, cùng số test như Step 1.

Nếu có test tìm phần tử theo class hoặc theo chuỗi cũ (ví dụ tìm text
`"Còn 50 cái"` ở dạng chữ thường thay vì badge), sửa test cho khớp giao diện
mới — nhưng **không** sửa test nào đang khẳng định hành vi bàn phím.

- [ ] **Step 5: Kiểm bằng tay**

Run: `pnpm dev`, mở `http://localhost:3000/pos`.
Expected: gõ tên hàng ra danh sách có khung và ảnh; `↑` `↓` di chuyển và dòng
đang chọn có thanh xanh bên trái; `Enter` thêm vào giỏ; `Esc` xoá ô tìm; `F2`
quay lại ô tìm. Chip danh mục cao rõ ràng, bấm được bằng ngón tay.

- [ ] **Step 6: Commit**

```bash
git add src/components/pos/product-search.tsx src/components/pos/category-grid.tsx tests/components/pos
git commit -m "style(pos): rebuild product search and category grid on the kit"
```

---

## Task 16: Áp kit vào `/admin/products`

**Files:**

- Modify: `src/app/admin/products/page.tsx`
- Modify: `src/app/admin/products/product-form.tsx`

**Interfaces:**

- Consumes: `PageHeader`, `DataTableShell`, `EmptyState`, `Money`, `StockBadge`, `ProductImage`, `CollapsibleFormCard`, `TouchButton` từ `@/components/kit`
- Produces: không đổi props công khai

- [ ] **Step 1: Viết lại `page.tsx`**

Thay toàn bộ phần `return` của `src/app/admin/products/page.tsx` bằng:

```tsx
  return (
    <div className="space-y-6">
      <PageHeader
        title="Sản phẩm"
        description="Hàng hoá đang bán tại quầy. Sửa ở đây là màn bán hàng đổi theo."
      />

      <ProductForm categories={categories} />

      <DataTableShell
        title="Danh sách"
        count={products.length}
        isEmpty={products.length === 0}
        empty={
          <EmptyState
            title="Chưa có sản phẩm nào"
            description="Thêm sản phẩm đầu tiên để bắt đầu bán hàng."
          />
        }
      >
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Tên</TableHead>
              <TableHead>Danh mục</TableHead>
              <TableHead className="text-right">Giá bán</TableHead>
              <TableHead className="text-right">Tồn</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {products.map((product) => (
              <TableRow key={product.id} className="hover:bg-accent/50">
                <TableCell>
                  <div className="flex items-center gap-3">
                    <ProductImage
                      src={product.imageUrl}
                      name={product.name}
                      size={40}
                    />
                    <div className="flex flex-col">
                      <span className="font-medium">{product.name}</span>
                      {product.aliases ? (
                        <span className="text-muted-foreground text-sm">
                          {product.aliases}
                        </span>
                      ) : null}
                    </div>
                  </div>
                </TableCell>
                <TableCell className="text-muted-foreground">
                  {product.category?.name ?? "—"}
                </TableCell>
                <TableCell className="text-right">
                  <Money amount={product.price} />
                </TableCell>
                <TableCell className="text-right">
                  <StockBadge stock={product.stock} unit={product.unit} />
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      </DataTableShell>
    </div>
  );
}
```

Thay khối import ở đầu file — bỏ `Badge`, `Card*`, `formatVnd`:

```tsx
import {
  DataTableShell,
  EmptyState,
  Money,
  PageHeader,
  ProductImage,
  StockBadge,
} from "@/components/kit";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import { prisma } from "@/server/db/prisma";

import { ProductForm } from "./product-form";
```

- [ ] **Step 2: Gập form lại và cho báo lưu có màu**

Trong `src/app/admin/products/product-form.tsx`, thay `<Card>…</Card>` bao
ngoài bằng `CollapsibleFormCard`, và đổi dòng thông báo. Phần `return` thành:

```tsx
  return (
    <CollapsibleFormCard title="Thêm sản phẩm" triggerLabel="Thêm sản phẩm">
      <form action={handleAction} className="grid grid-cols-2 gap-4">
        {/* giữ nguyên toàn bộ các trường đã có, không đổi name và id */}
      </form>
    </CollapsibleFormCard>
  );
}
```

Nghĩa là: giữ y nguyên mọi `<div className="col-span-2 space-y-1.5">…</div>`
đang có (tên, tên gọi khác, mã nội bộ, danh mục, đơn vị, tồn kho, giá bán,
giá vốn, ảnh sản phẩm), chỉ thay hai lớp bọc `Card`/`CardHeader`/`CardTitle`/
`CardContent` bằng `CollapsibleFormCard`.

Rồi thay khối nút submit ở cuối form bằng:

```tsx
<div className="col-span-2 flex items-center gap-3">
  <TouchButton type="submit">Thêm sản phẩm</TouchButton>
  {message ? (
    <span
      className={
        message === "Đã lưu"
          ? "text-success text-sm font-medium"
          : "text-destructive text-sm font-medium"
      }
    >
      {message}
    </span>
  ) : null}
</div>
```

Cập nhật import — bỏ `Button`, `Card*`, thêm:

```tsx
import {
  CollapsibleFormCard,
  ImagePicker,
  TouchButton,
} from "@/components/kit";
```

- [ ] **Step 3: Chạy toàn bộ kiểm tra**

Run: `pnpm check`
Expected: lint, typecheck, test đều xanh.

- [ ] **Step 4: Kiểm bằng tay**

Run: `pnpm dev`, mở `http://localhost:3000/admin/products`.
Expected: trang mở ra thấy ngay bảng (form đã gập); bấm "Thêm sản phẩm" thì
form mở; lưu xong hiện chữ "Đã lưu" màu xanh lá; bảng có ảnh, giá canh phải,
badge tồn kho ba màu; xoá hết sản phẩm trong DB thì hiện trạng thái rỗng.

- [ ] **Step 5: Commit**

```bash
git add src/app/admin/products
git commit -m "style(admin): rebuild the products page on the kit"
```

---

## Task 17: Kiểm chứng bằng trình duyệt thật

**Files:**

- Create: `e2e/kit-gallery.spec.ts`
- Create: `e2e/admin-product-image.spec.ts`

**Interfaces:**

- Consumes: cấu hình đăng nhập sẵn có trong `e2e/` (xem `e2e/admin-products.spec.ts` để lấy đúng cách đăng nhập)
- Produces: ảnh chụp trong `playwright-report/`

- [ ] **Step 1: Viết spec chụp gallery ở hai theme**

Tạo `e2e/kit-gallery.spec.ts`:

```ts
import { expect, test } from "@playwright/test";

test.describe("UI kit gallery", () => {
  for (const scheme of ["light", "dark"] as const) {
    test(`hien day du o theme ${scheme}`, async ({ page }) => {
      await page.emulateMedia({ colorScheme: scheme });
      await page.goto("/dev/kit");

      await expect(
        page.getByRole("heading", { level: 1, name: "UI Kit" }),
      ).toBeVisible();
      await expect(page.getByText("Hết hàng", { exact: true })).toBeVisible();
      await expect(page.getByText("Còn 50 cái")).toBeVisible();

      await page.screenshot({
        path: `playwright-report/kit-${scheme}.png`,
        fullPage: true,
      });
    });
  }
});
```

- [ ] **Step 2: Chạy spec**

Run: `pnpm test:e2e e2e/kit-gallery.spec.ts`
Expected: PASS — 2 test, và hai file ảnh trong `playwright-report/`.

- [ ] **Step 3: Viết spec upload ảnh**

Mở `e2e/admin-products.spec.ts` và đọc phần đăng nhập của nó — repo này gác
`/admin` bằng cookie phiên, nên spec mới phải đăng nhập y hệt. Chép đúng khối
đăng nhập đó vào chỗ đánh dấu bên dưới.

Tạo `e2e/admin-product-image.spec.ts`:

```ts
import { expect, test } from "@playwright/test";

test("upload anh san pham roi thay no o /pos", async ({ page }) => {
  // <- Cac buoc dang nhap chep tu e2e/admin-products.spec.ts dat o day.
  await page.goto("/admin/products");

  await page.getByRole("button", { name: "Thêm sản phẩm" }).first().click();
  await page.getByLabel("Tên sản phẩm").fill("Bugi thử ảnh");
  await page.getByLabel("Giá bán (VND)").fill("15000");

  // Anh 1x1 PNG mau do, nhung truc tiep de khong phai them file vao repo.
  await page.setInputFiles('input[type="file"]', {
    name: "anh.png",
    mimeType: "image/png",
    buffer: Buffer.from(
      "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==",
      "base64",
    ),
  });

  await page.getByRole("button", { name: "Thêm sản phẩm" }).last().click();
  await expect(page.getByText("Đã lưu")).toBeVisible();

  await page.goto("/pos");
  await page.getByRole("combobox").fill("Bugi thử ảnh");
  await expect(page.getByRole("img", { name: "Bugi thử ảnh" })).toBeVisible();
});
```

Dòng đánh dấu ở đầu test phải được thay bằng các bước đăng nhập thật trước khi
chạy, nếu không `page.goto("/admin/products")` sẽ bị đá về `/login`.

- [ ] **Step 4: Chạy spec**

Run: `pnpm test:e2e e2e/admin-product-image.spec.ts`
Expected: PASS.

- [ ] **Step 5: Chạy toàn bộ e2e để chắc không làm vỡ luồng cũ**

Run: `pnpm test:e2e`
Expected: PASS toàn bộ, kể cả `pos-offline-sale.spec.ts`.

- [ ] **Step 6: Commit**

```bash
git add e2e/kit-gallery.spec.ts e2e/admin-product-image.spec.ts
git commit -m "test(e2e): cover the kit gallery and product image upload"
```

---

## Task 18: Đợt 2 — lan kit ra phần còn lại

Chỉ làm sau khi Task 1–17 đã xanh. Mỗi file là một commit riêng để dễ lùi.

**Files:**

- Modify: `src/components/pos/cart-panel.tsx`
- Modify: `src/components/pos/cart-line-row.tsx`
- Modify: `src/components/pos/payment-dialog.tsx`
- Modify: `src/components/pos/held-orders-bar.tsx`
- Modify: `src/components/pos/sync-indicator.tsx`
- Modify: `src/app/admin/categories/page.tsx`
- Modify: `src/app/admin/orders/page.tsx`
- Modify: `src/app/admin/debts/page.tsx`
- Modify: `src/app/admin/reports/page.tsx`
- Modify: `src/app/admin/settings/page.tsx`

**Interfaces:**

- Consumes: toàn bộ `@/components/kit`
- Produces: không đổi props công khai của file nào

- [ ] **Step 1: Thay mọi chỗ hiển thị tiền bằng `Money`**

Run: `grep -rn "formatVnd" src/components/pos src/app/admin`

Với **mỗi** kết quả nằm trong JSX, thay `{formatVnd(x)}` bằng `<Money amount={x} />`.
Riêng "tiền thối lại" trong `payment-dialog.tsx` dùng `<Money amount={x} size="display" />`
— Spec 1 mục 5 đòi "chữ rất to".

Giữ nguyên mọi lời gọi `formatVnd` **ngoài** JSX (ví dụ dựng chuỗi cho
`aria-label` hay nội dung QR).

Run: `pnpm vitest run` sau mỗi file. Commit từng file:

```bash
git add src/components/pos/cart-panel.tsx
git commit -m "style(pos): use the kit Money component in the cart"
```

- [ ] **Step 2: Thay mọi nút bấm trên `/pos` bằng `TouchButton`**

Run: `grep -rn "<Button" src/components/pos`

Với mỗi kết quả, đổi `<Button` thành `<TouchButton` và sửa import. Đây là các
nút thu ngân bấm cả ngày, đều phải đạt ô chạm.

Run: `pnpm vitest run tests/components/pos`
Expected: PASS.

```bash
git add src/components/pos
git commit -m "style(pos): give every sales-screen button a 44px touch target"
```

- [ ] **Step 3: Thêm `PageHeader` cho mọi trang admin**

Với mỗi file trong `src/app/admin/*/page.tsx`, thay thẻ `<h1>` trần bằng
`<PageHeader title="…" description="…" />`. Mô tả viết bằng tiếng Việt, một
dòng, nói trang này để làm gì.

```bash
git add src/app/admin
git commit -m "style(admin): give every admin page a PageHeader"
```

- [ ] **Step 4: Thêm trạng thái rỗng cho mọi bảng admin**

Với mỗi bảng trong `src/app/admin/*/page.tsx`, bọc bằng `DataTableShell` với
`empty={<EmptyState title="…" />}`, giống hệt Task 16 Step 1.

```bash
git add src/app/admin
git commit -m "style(admin): show an empty state instead of a bare table"
```

- [ ] **Step 5: Dùng `StatTile` cho trang báo cáo**

Trong `src/app/admin/reports/page.tsx`, thay các ô số liệu tự chế bằng
`<StatTile label="…" value={…} format="money" />` cho các con số tiền và
`format="number"` cho các con số đếm.

```bash
git add src/app/admin/reports/page.tsx
git commit -m "style(admin): use StatTile on the reports page"
```

- [ ] **Step 6: Chạy toàn bộ kiểm tra lần cuối**

Run: `pnpm check && pnpm build && pnpm test:e2e`
Expected: cả ba đều xanh.

- [ ] **Step 7: Xem lại bằng mắt lần cuối**

Run: `pnpm dev`, đi qua `/pos`, `/admin/products`, `/admin/reports`, `/dev/kit`
ở cả light lẫn dark. Đối chiếu với ảnh chụp từ Task 17.
Expected: không màn nào còn ô xám trống, không nút nào thấp hơn 44px trên
`/pos`, không bảng nào trống trơn không lời dẫn.

- [ ] **Step 8: Commit cuối**

```bash
git add -A
git commit -m "style: finish rolling the kit across POS and admin"
```
